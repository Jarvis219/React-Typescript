export const Hobby = (props: any) => {
  // const { hobbies } = props;
  return (
    <div>
      aa
      {/* <ul>
        {hobbies.map((item: any, index: number) => (
          <li key={index}>{item.title}</li>
        ))}
      </ul> */}
    </div>
  );
};
