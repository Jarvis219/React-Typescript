export enum ProductStatus {
  public = "public",
  private = "private",
  delete = "delete",
}

export enum ProductPagination {
  plus = "PLUS",
  minus = "MINUS",
}

export enum DisableProductPagination {
  plusMax = "PLUS_MAX",
  minusMin = "MINUS_Min",
}
